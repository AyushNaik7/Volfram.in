{
  "buildCommand": "cd Frontend/volform && npm run build",
  "outputDirectory": "Frontend/volform/dist",
  "installCommand": "cd Frontend/volform && npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
